apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  audit:
    profile: Default
  servingCerts:
    namedCertificates:
    - names:
      - api.ppe02k8s.bsc.bscal.com
      servingCertificate:
        name: api-certs

