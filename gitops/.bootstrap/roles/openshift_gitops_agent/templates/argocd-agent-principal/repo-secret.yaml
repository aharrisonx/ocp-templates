kind: Secret
apiVersion: v1
metadata:
  name: repo-ssh
  namespace: "{{ gitops_instance_namespace }}"
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  sshPrivateKey: |
    {{ lookup('ansible.builtin.file', gitops_ssh_key_file) | indent(4) }}
  type: git
  url: "{{ git_src }}"
