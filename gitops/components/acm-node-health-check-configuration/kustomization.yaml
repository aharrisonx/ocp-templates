apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-workload-availability

resources:
- node-health-check.yaml
- rbac.yaml
#- custom-config-map.yaml
- service-account.yaml
- daemonset.yaml
- pvc.yaml
#- node-taints-on-node-condition-policy.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: node-problem-detector-custom-config
  namespace: openshift-workload-availability
  files:
  - bond0-check.sh
  - bond0-test.json
  - ontap-san-check.sh
  - ontap-san-test.json

# helmCharts:
#   - name: node-problem-detector
#     namespace: openshift-workload-availability
#     releaseName: node-problem-detector
#     repo: https://charts.deliveryhero.io/
#     valuesFile: values.yaml