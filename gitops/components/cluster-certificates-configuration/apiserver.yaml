apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  audit:
    profile: Default
  servingCerts:
    namedCertificates:
    - names: 
      - api.${CLUSTER_BASE_DOMAIN}
      servingCertificate:
        name: cert-manager-api-cert-secret
 

